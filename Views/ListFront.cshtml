@model IEnumerable<Pi2021.Models.Evenement>

@{
    ViewBag.Title = "ListFront";
}

<h2>ListFront</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nameEvt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adresseEvt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateEvt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.stateEvt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.image)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.statusEvt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nameEvt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.adresseEvt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateEvt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.stateEvt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.image)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.statusEvt)
        </td>
        <td>
            @Html.ActionLink("Commenter", "List", new { id=item.id }) 

        </td>
    </tr>
}

</table>
